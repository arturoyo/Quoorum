{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd $VERCEL_ROOT && pnpm build --filter=@quoorum/web",
  "installCommand": "cd $VERCEL_ROOT && pnpm install",
  "framework": "nextjs",
  "outputDirectory": ".next"
}
